apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: kbox
  labels: { app: app }
spec:
  selector: { app: app }
  ports:
    - name: http
      port: 80            # cluster-internal port
      protocol: TCP
      targetPort: 8080    # pod listens on 8080 (nginx-unprivileged)
